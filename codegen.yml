overwrite: true
schema: "./result.graphql"
generates:
  schemas.ts:
    plugins:
      - ./dist/index.js:
          defaultRequiredMessage: "This field have generic message"
          onlyWithConstrain: false
