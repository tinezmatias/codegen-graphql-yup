overwrite: true
schema: "./result.graphql"
generates:
  schemas.ts:
    plugins:
      - ./dist/index.js:
          defaultRequiredMessage: "Este campo es requerido"
          onlyWithConstrain: true
